{
  "name": "ai-hots-functions",
  "engines": {
    "node": "^20"
  },
  "type": "module",
  "private": true,
  "scripts": {
    "build": "echo no build",
    "serve": "firebase emulators:start --only functions,hosting",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^12.5.0",
    "firebase-functions": "^5.0.1",
    "node-fetch": "^3.3.2"
  }
}
